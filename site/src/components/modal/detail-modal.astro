---
import Modal from "~/components/modal/modal.astro";

interface Props {
	id: string;
	title: string;
	icon?: `lucide:${string}` | `svg-spinners:${string}`;
	iconClass?: string;
	modalClass?: string;
}

const { id, title, icon, iconClass, modalClass } = Astro.props;
---

<Modal {id} {title} {icon} {iconClass} {modalClass}>
	<slot />
	<div class="modal-action">
		<form method="dialog">
			<button class="btn btn-sm btn-neutral">Tutup</button>
		</form>
	</div>
</Modal>
