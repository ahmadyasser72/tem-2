<% const roomLabel = it.roomNumber ? `Kamar ${it.roomNumber} - ` : "" %>*🎉 Selamat Datang di <%= roomLabel %><%= it.config.siteName %>!*

Halo *<%= it.fullName %>*,

Data Anda telah terdaftar sebagai penghuni indekos.

*📋 Tagihan Sewa Pertama*
💰 Jumlah: <%= it.amount %>
📅 Jatuh Tempo: <%= it.dueDate %>

Untuk mengaktifkan akun Anda, silakan lakukan pembayaran:
<% if (it.paymentUrl) { %>💳 *<%= it.paymentUrl %>*
<% } %>

_Pesan ini dikirim otomatis oleh sistem._
