---
import Icon from "~/components/ui/icon.astro";

interface Props {
	id: string | number;
	triggerClass?: string;
	triggerLabel?: string;
	triggerIcon?: `lucide:${string}` | `svg-spinners:${string}`;
	menuClass?: string;
}

const {
	id,
	triggerClass = "",
	triggerLabel,
	triggerIcon = "lucide:chevron-up" as const,
	menuClass = "w-36",
} = Astro.props;
---

<button
	class:list={["btn btn-outline btn-xs", triggerClass]}
	popovertarget={`aksi-${id}`}
>
	<Icon name={triggerIcon} size="size-3.5" />
	{triggerLabel && <span>{triggerLabel}</span>}
</button>
<ul
	id={`aksi-${id}`}
	class:list={[
		"menu dropdown dropdown-end dropdown-top mb-1 menu-sm rounded-box border border-base-300 bg-base-100 shadow-sm",
		menuClass,
	]}
	popover
>
	<slot />
</ul>
