@startuml
|Staff / Owner|
start
:Buka menu komplain;

|Sistem|
:Verifikasi akun;

if (Valid?) then (ya)
  :Ambil data komplain;
  :Tampilkan daftar komplain;
  
  |Staff / Owner|
  :Lihat detail komplain;
  if (Role Staff?) then (ya)
    :Ubah status (diproses/selesai);
    
    |Sistem|
    :Ubah status di Basis Data;
    :Tampilkan notifikasi sukses;
  else (tidak)
  endif
else (tidak)
  :Tolak akses;
endif

stop
@enduml
